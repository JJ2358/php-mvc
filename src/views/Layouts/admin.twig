{% extends 'base.twig' %}

{% block content %}
<div class="admin-bar">
    <a href="/admin">Dashboard</a>
    <a href="/logout">Logout</a>
</div>

{% if message %}
    <p>{{ message }}</p>
{% endif %}

<form action="/admin/fetch-jobs" method="post">
    <button type="submit">Fetch Jobs from API</button>
</form>
{% endblock %}
