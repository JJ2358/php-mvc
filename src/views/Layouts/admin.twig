{% extends 'base.twig' %}

{% block content %}
<div class="container mx-auto my-8">
    <h1 class="text-3xl font-bold mb-4 text-center">Admin Dashboard</h1>
    <form method="post" action="/admin/fetch-jobs">
        <button type="submit" class="px-4 py-2 bg-green-500 text-white rounded-md hover:bg-green-700">Fetch Jobs from API</button>
    </form>
    <div class="mt-4">
        {{ getSessionMessage() }}
    </div>
</div>
{% endblock %}
